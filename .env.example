# Server-only — used by api/career-match.ts. Do NOT prefix with VITE_,
# NEXT_PUBLIC_, or REACT_APP_: any of those prefixes get compiled into
# the public client-side JS bundle and would leak the key.
OPENROUTER_API_KEY=your_key_here
